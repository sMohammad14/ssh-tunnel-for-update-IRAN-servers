# آپدیت و به روز رسانی سرور ایران با سرور خارج از طریق ارتباط SSH



با توجه به اختلال های اینترنت و البته تحریم هایی که روی IP های ایران هست، برای آ
برای آپدیت و دسترسی به منابع از طریف سرورهایی ایران معمولا دچار مشکل و خطا میشیم.
با روشی که در پایین توضیح میدم، میتونید به راحتی از طریق یک سرور خارج که سرور ایران بهش دسترسی SSH داره، آپدیت ها و ... رو از اون سرور بگیرید.
این روش ساده هست و بعد از یک بار اجرا، میتونید به طور همیشگی روی سرورتون ازش استفاده کنید و مانند یک نرم افزار، خاموش و روشنش کنید.


توی سرور خارج این خط ها رو تک به تک کپی و پیست کنید و Enter بزنید:

```

sudo su

```

```

echo "PermitRootLogin yes" >> /etc/ssh/sshd_config

```

```

echo "PermitTunnel yes" >> /etc/ssh/sshd_config

```

```

echo "net.ipv4.ip_forward=1" >> /etc/sysctl.conf

```

```

sysctl -p

```

```

service ssh restart

```

```

iptables -t nat -A POSTROUTING -o $(ip route | grep default | awk '{print $5}') -j MASQUERADE

```

آی پی سرور خارج رو میخواییم، اگر نمیدونید، با این کد IP برای شما نشون داده میشه:

```

curl ifconfig.me

```
مثلا IP هست:
`2.2.2.2`

حالا روی سرور ایران:
```

sudo -i

```
```

ssh-keygen -t rsa -b 4096 -N "" -f ~/.ssh/id_rsa

```
```

ssh-copy-id root@2.2.2.2

```
ازتون ```رمز عبور سرور خارج رو می پرسه```

## حالا برای سازوکار خاموش و روشن کردن ارتباط و مشاهده وضعیت فعلی VPN، توی سرور ایران به این طریق عمل کنید که در ادامه گفتم:

کد زیر رو بزنید، دقت کنید به جای 2.2.2.2 باید IP سرور خارج خودتون رو جایگزین کنید:
### در سرور ایران
```

SERVER_IP="2.2.2.2"


```


اسکریپت روش کردن VPN:
```
cat > /usr/local/bin/vpn-on << EOF
#!/bin/bash
/usr/bin/sshuttle --dns -r root@$SERVER_IP 0/0 --daemon --pid /var/run/vpn.pid --ssh-cmd "ssh -o StrictHostKeyChecking=no"
sleep 2
echo "VPN is 🟢"
echo "Your IP Is:" \$(curl -s ifconfig.me)
EOF
```
اسکریپت خاموش کردن VPN:
```
cat > /usr/local/bin/vpn-off << EOF
#!/bin/bash
if [ -f /var/run/vpn.pid ]; then
    kill \$(cat /var/run/vpn.pid) 2>/dev/null
    rm -f /var/run/vpn.pid
fi
pkill -f sshuttle 2>/dev/null
echo "VPN is 🔴"
echo "Your IP Is:" \$(curl -s ifconfig.me)
EOF
```
اسکریپت مشاهده وضعیت VPN:
```
cat > /usr/local/bin/vpn-status << 'EOF'
#!/bin/bash
if [ -f /var/run/vpn.pid ] && ps -p $(cat /var/run/vpn.pid) > /dev/null 2>&1; then
    echo "VPN Is 🟢"
    echo "Your IP Is: $(curl -s ifconfig.me)"
else
    echo "VPN Is 🔴"
fi
EOF
```
اینجا کار تموم شد حالا توی سرور ایران میتونید با کامندهای زیر، این ارتباط رو کنترل کنید:


### 1. روشن کردن VPN
```

vpn-on
```
### 2. خاموش کردن VPN
```

vpn-off
```
### 3. وضعیت VPN
```

vpn-status
```
